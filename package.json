{
  "name": "easy-digital-gestionale",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "dev:backend": "npm --workspace @eda/backend run start:dev",
    "dev:flask": "cd manager-flask && python3 app.py",
    "build": "npm -ws run build",
    "test": "npm -ws run test"
  }
}
