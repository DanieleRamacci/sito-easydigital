name: Build and Push to GHCR

on:
  push:
    branches: ["main"]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata (WordPress)
        id: meta_wp
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ github.event.repository.name }}
          tags: |
            type=raw,value=latest
            type=sha

      - name: Build and push (WordPress)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.meta_wp.outputs.tags }}
          labels: ${{ steps.meta_wp.outputs.labels }}

      - name: Docker metadata (Manager Flask)
        id: meta_mgr_flask
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ github.event.repository.name }}-manager-flask
          tags: |
            type=raw,value=latest
            type=sha

      - name: Build and push (Manager Flask)
        uses: docker/build-push-action@v6
        with:
          context: ./manager-flask
          file: manager-flask/Dockerfile
          push: true
          tags: ${{ steps.meta_mgr_flask.outputs.tags }}
          labels: ${{ steps.meta_mgr_flask.outputs.labels }}
